cmake_minimum_required(VERSION 3.16)

project(qtlottieviewer VERSION 0.1 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD_REQUIRED ON)

find_package(Qt6 6.5 REQUIRED COMPONENTS Quick Qml)

qt_standard_project_setup(REQUIRES 6.5)

qt_add_executable(appqtlottieviewer
    main.cpp
)

set(default_lottie_file "../../baseline/data/docsamples/mod/logo.json")
set_source_files_properties(${default_lottie_file} PROPERTIES
    QT_RESOURCE_ALIAS default_file.json
)

qt_add_qml_module(appqtlottieviewer
    URI qtlottieviewer
    VERSION 1.0
    QML_FILES
        Main.qml
    RESOURCES
        ${default_lottie_file}
    RESOURCE_PREFIX
        /
)

# Qt for iOS sets MACOSX_BUNDLE_GUI_IDENTIFIER automatically since Qt 6.1.
# If you are developing for iOS or macOS you should consider setting an
# explicit, fixed bundle identifier manually though.
set_target_properties(appqtlottieviewer PROPERTIES
#    MACOSX_BUNDLE_GUI_IDENTIFIER com.example.appqtlottieviewer
    MACOSX_BUNDLE_BUNDLE_VERSION ${PROJECT_VERSION}
    MACOSX_BUNDLE_SHORT_VERSION_STRING ${PROJECT_VERSION_MAJOR}.${PROJECT_VERSION_MINOR}
    MACOSX_BUNDLE TRUE
    WIN32_EXECUTABLE TRUE
)

target_link_libraries(appqtlottieviewer
    PRIVATE Qt6::Quick
)

include(GNUInstallDirs)
install(TARGETS appqtlottieviewer
    BUNDLE DESTINATION .
    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
)
